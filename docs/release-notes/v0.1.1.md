# v0.1.1 Release Notes Draft

Use these notes after the OSS approval polish PR is merged and a `v0.1.1` tag is created.

## Highlights

- Added a synthetic sample report payload for YouTube, TikTok, and Instagram.
- Added troubleshooting docs for provider diagnostics and empty report sections.
- Added reviewer evidence, support guidance, CODEOWNERS, issue template config, and Makefile shortcuts.
- Added a synthetic visual report preview in `docs/assets/sample-report.svg`.
- Clarified GHCR image usage for the current `main` image and the next tagged release image.
- Updated promotion guidance with a concrete ethical visibility plan.

## Validation

- CI should pass on the merge commit.
- Docker image workflow should publish `ghcr.io/kirillsaven/competitors_spy:v0.1.1` after the tag is pushed.

## Suggested Commands

```bash
git checkout main
git pull --ff-only origin main
git tag -a v0.1.1 -m "v0.1.1 OSS approval polish"
git push origin v0.1.1
gh release create v0.1.1 --repo kirillsaven/competitors_spy \
  --title "v0.1.1 OSS approval polish" \
  --notes-file docs/release-notes/v0.1.1.md
```
